import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.johnson.brokeandcold',
  appName: 'Broke And Cold',
  webDir: 'dist/brokeandcold-ng/browser/'
};

export default config;
